---
kind: pipeline
type: exec
name: default

platform:
  os: linux
  arch: amd64

steps:
- name: greeting
  commands:
  - echo hello world
  - echo hello world 2
- name: test docker
  environment:
    PASSWORD:
      from_secret: SCAN_PASS
    USERNAME:
      from_secret: SCAN_USER
  commands:
  - docker pull zookeeper:latest
...
